name: Container
on: push

jobs:
  node-docker:
    runs-on: ubuntu-latest
    container: debian:bullseye
    steps:
      - name: log node version
        run: |
          cat /etc/os-release
      - name: Check dpkg
        run: dpkg --version
      - name: check rpm  
        run: rpm --version
        if: always()